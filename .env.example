# Postgres connection used by the contact form API route (/api/contact).
# On the server, copy the real value into .env.local (which is gitignored and
# must NEVER be committed). The portfolio database is local to the VPS.
DATABASE_URL=postgres://portfolio_user:CHANGE_ME@127.0.0.1:5432/portfolio
